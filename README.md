# FileCopyPython
A file moving/coppying application for my father's business


Maintinence notes to self: 
- use auto-py-to-exe in cmd prompt to recompile.
- uses tkinter and auto-py-to-exe (tkinter is standard, auto-py-to-exe can be redownloaded through pip)